{
  "name": "subtitle-search-frontend",
  "private": true,
  "type": "module",
  "comment": "Upper bounds are majors, not favourites, exactly as in pyproject.toml: the floor is what the code needs, the ceiling is the next release allowed to break it. package-lock.json is committed so the graph is fixed rather than merely bounded, and subtitle_search/web is committed so installing the tool never runs any of this.",
  "engines": { "node": ">=20.19" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": ">=19 <20",
    "react-dom": ">=19 <20"
  },
  "devDependencies": {
    "@vitejs/plugin-react": ">=6 <7",
    "vite": ">=8 <9"
  }
}
